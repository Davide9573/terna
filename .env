# Host port exposed by the frontend container
TERNA_PORT=5150

# Public URL allowed by backend CORS (comma-separated if multiple origins)
CORS_ALLOW_ORIGINS=https://terna.masciotta.casa

# API abuse protection (requests per window, per client IP)
RATE_LIMIT_REQUESTS=120
RATE_LIMIT_WINDOW_SECONDS=60
